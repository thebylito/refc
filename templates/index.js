const rcc = require('./rcc');
const rcr = require('./rcr');
const duck = require('./duck');
const saga = require('./saga');
const styles = require('./styles');

module.exports = {
  rcc,
  rcr,
  duck,
  saga,
  styles,
};
